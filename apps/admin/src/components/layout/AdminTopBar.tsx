"use client";
// TopBar is now integrated into AuthGuard as part of the Mac window chrome.
// This stub is kept so existing imports don't break.
export default function AdminTopBar({ title }: { title?: string }) { return null; }
